#include <stdio.h>
#include <stdlib.h>
int main () {
	int i, ai [] = {2,4,6,8};
	int *pi, *p2; // equivalente a int *pi

	pi = ai;
	for (i=0; i<4; i++)
		printf ("%d\n", pi [i]);

	p2 = (int *)malloc (5 * sizeof (int));
	for (i=0; i<5; i++)
		scanf ("%d", &pi [i]);
	

	return 0;
}
