int i = 10;
int* pi;   // equivalente a int *pi
           // pi es una var. int* (puntero a int)
pi = &i;   // var. int* (puntero a int) o *pi es int
pi = &i;   // pi contiene la direcciOn de i
*pi = 10;  // escribe en la direcciOn guardada por pi
